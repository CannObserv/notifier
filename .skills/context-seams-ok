# Acknowledged cross-reference seams (check-seams.sh --ack-file).
# One entry per judged line, form "PATH :: CONTENT" — content is a substring of
# the live line, so the entry stops matching (and gets re-judged) the moment
# that line changes. Reviewed 2026-08-27 under #42: every anchor, path, and
# claim below was verified against the current tree.

# docs/ARCHITECTURE.md — legitimate links back to the policy file's index
# sections; both anchors (## Project Layout, ## Detail Docs) exist in AGENTS.md.
docs/ARCHITECTURE.md :: are summarised in [AGENTS.md](../AGENTS.md#project-layout)
docs/ARCHITECTURE.md :: [AGENTS.md § Detail Docs](../AGENTS.md#detail-docs)

# docs/ARCHITECTURE.md — names the tenancy rule as stated in AGENTS.md
# (§ API Boundary Principles: tenancy enforced at the auth layer, business
# logic never branches on it); tests/api/test_tenant_isolation.py exists.
docs/ARCHITECTURE.md :: The rule AGENTS.md states — tenancy is enforced at the boundary

# docs/SKILLS.md — describes the context-budget write guard; hook file,
# uninstall path, and both budget values (6000 / 10000) verified against
# .claude/hooks/ and .skills/context-*budget.
docs/SKILLS.md :: an edit pushes `AGENTS.md` past 6,000 tokens

# docs/SKILLS.md — skills-inventory row naming the curating-context trigger
# phrases; "trim AGENTS.md" is a trigger, not a stale pointer.
docs/SKILLS.md :: Triggers: `curate context`, `context budget`, `trim AGENTS.md`

# docs/SOCRATICODE.md — this doc IS the detail doc for the
# ## Code Exploration Policy block in AGENTS.md (block exists, AGENTS.md:36);
# all four mentions describe that relationship or which variant the block is on.
docs/SOCRATICODE.md :: Detail doc for the `## Code Exploration Policy` block in `AGENTS.md`
docs/SOCRATICODE.md :: heading down there, not in `AGENTS.md`.
docs/SOCRATICODE.md :: `AGENTS.md` block should be on its degraded variant.
docs/SOCRATICODE.md :: block in `AGENTS.md` is therefore on **variant A** (standard).
# Judged 2026-09-12 during the #57 curation.
# AGENTS.md's API Boundary Principles did NOT move in this run, so these two
# pointers still resolve to the section they name.
docs/reference/monitors.md :: taxonomy is the thing AGENTS.md's API Boundary Principles forbid.
docs/reference/monitors.md :: [AGENTS.md § API Boundary Principles](../../AGENTS.md#api-boundary-principles)
# Pre-existing heading, untouched by this run. Renaming it would break any
# anchor already pointing at it, for no gain this curation needs.
docs/DEPLOYMENT.md :: The VM split (#43, done)
# Judged 2026-09-12, #57 curation, against --file docs/DEPLOYMENT.md.
# This run only ADDED to DEPLOYMENT.md; nothing left it, so every pointer at it
# still resolves to what it names.
docs/SOCRATICODE.md :: | `deployment-architecture` | `docs/DEPLOYMENT.md` |
docs/reference/monitors.md :: - [DEPLOYMENT.md](../DEPLOYMENT.md) — installing and enabling the timers
docs/reference/tailscale.md :: to [DEPLOYMENT.md](../DEPLOYMENT.md).

# Reviewed 2026-09-19 under #74. This run demoted three blocks out of
# § Infrastructure (co-index, the exe.dev proxy gate, the watcher consumer).
# None of the back-references below points at any of them — each names either
# the policy file generically or a section that is still inline, verified:
# § Code Exploration Policy (negative rule), § Conventions (ISO 8601 with Z),
# § API Boundary Principles. Legitimate, not defects.
.claude/hooks/context-budget-guard.sh :: AGENTS.md, the next fortnight of edits puts it back
.claude/hooks/context-budget-guard.sh :: the policy file: AGENTS.md or CLAUDE.md at the repo root
.claude/hooks/context-budget-guard.sh :: CLAUDE.md is followed to its target
.claude/hooks/context-budget-guard.sh :: AGENTS.md|CLAUDE.md) KIND="policy"
.claude/hooks/skills-submodule-update.sh :: the exit-2 of AGENTS.md's gate
.claude/hooks/socraticode-health.sh :: repo's AGENTS.md.
.claude/hooks/socraticode-health.sh :: told by AGENTS.md to prefer codebase_search over grep
scripts/rotate_key.py :: AGENTS.md fixes ISO 8601 with a ``Z`` suffix
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: referenced from project docs (e.g., AGENTS.md, README.md)
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: "AGENTS.md"
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: AGENTS.md: project structure, conventions
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: check AGENTS.md and README.md, never the
src/api/schemas/monitor.py :: (AGENTS.md, API Boundary
tests/api/test_dispatch_route.py :: AGENTS.md: reject 422 with a clear field path on miss
tests/api/test_dispatch_route.py :: an unbound reference fails loudly, per AGENTS.md
tests/api/test_monitors_route.py :: (AGENTS.md, API Boundary Principles)
tests/api/test_tenant_isolation.py :: AGENTS.md puts it as a design rule
tests/deploy/test_load_env.py :: "AGENTS.md",

# The last two policy-file back-references: one uses AGENTS.md purely as a
# tracked-file control, the other names the ISO 8601 rule that is still inline.
tests/deploy/test_socraticode_config.py :: TRACKED_CONTROL = "AGENTS.md"
tests/test_rotate_key.py :: AGENTS.md mandates YYYY-MM-DDTHH:MM:SS.ffffffZ

# Per-doc sweeps for the two split destinations. This run only ADDED sections to
# both files; every reference below names the file as a whole — an index entry, a
# context-artifact manifest path, a hook's existence check — so none of them can
# be broken by an append.
docs/reference/monitors.md :: [tailscale.md](tailscale.md) — the ACL rule a reporting node needs first
docs/reference/tailscale.md :: `docs/reference/tailscale.md` (what a tailnet is, WireGuard, MagicDNS
docs/SOCRATICODE.md :: | `code-exploration` | `docs/SOCRATICODE.md` | this file
.claude/hooks/socraticode-health.sh :: [ -f "$PROJECT/docs/SOCRATICODE.md" ]
.claude/hooks/socraticode-reminder.sh :: re-run this hook by hand for the ToolSearch
.claude/hooks/socraticode-reminder.sh :: generated `docs/SOCRATICODE.md` — points here instead of transcribing it
.socraticodecontextartifacts.json :: "path": "./docs/SOCRATICODE.md",
scripts/install_qdrant_key.sh :: # docs/SOCRATICODE.md and CannObserv/notifier#57.
