# Judged 2026-09-12 during the #57 curation.

# The two packages ARE the boundary the sentence defines, and the next clause
# names both. It cannot change without the sentence being rewritten.
AGENTS.md :: stable :: Two packages, one boundary:

# The table immediately above lists exactly the sweeps being counted.
AGENTS.md :: enumerated :: The two sweeps are the only thing watching for consumer silence.

# The numbered list directly beneath is the enumeration.
AGENTS.md :: enumerated :: Two env files, loaded in order (later values override):

# tests/ci/test_dependencies.py is named in the sentence itself and asserts
# each rule; docs/ARCHITECTURE.md#dependency-policy carries them written out.
AGENTS.md :: gated :: **Dependencies:** four rules, all asserted by
