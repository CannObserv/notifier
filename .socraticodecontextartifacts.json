{
  "artifacts": [
    {
      "name": "code-exploration",
      "path": "./docs/SOCRATICODE.md",
      "description": "SocratiCode exploration reference \u2014 full tool table, the ToolSearch prefetch query, per-tool notes, graph-health and index-scope guidance, plus this repo's measured graph yield and artifact inventory."
    },
    {
      "name": "database-schema",
      "path": "./alembic/versions",
      "description": "PostgreSQL schema (Alembic migrations). Authoritative source for table definitions, columns, indexes, constraints, foreign keys, and JSONB/ARRAY column shapes."
    },
    {
      "name": "deployment-architecture",
      "path": "./docs/DEPLOYMENT.md",
      "description": "Production deployment topology \u2014 systemd service (notifier on :9000), port assignments, env file layout (/etc/notifier/.env vs repo .env), co-location with watcher service, and lifecycle commands."
    },
    {
      "name": "operational-runbook",
      "path": "./docs/COMMANDS.md",
      "description": "Operational command reference \u2014 uv/pytest/ruff/alembic invocations, dev server vs systemd workflow, env loading, and database migration procedures."
    },
    {
      "name": "systemd-unit",
      "path": "./deploy",
      "description": "systemd units for the notifier service \u2014 notifier.service (production, port 9000) and notifier-dev.service (dev endpoint, port 9001, notifier_dev). User, ExecStart command, EnvironmentFile sources, restart policy, and which unit carries the production-database opt-in. Authoritative for runtime config."
    }
  ]
}
